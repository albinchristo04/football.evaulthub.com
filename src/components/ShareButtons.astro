---
interface Props {
  matchName: string;
  matchUrl: string;
}

const { matchName, matchUrl } = Astro.props;

const encodedText = encodeURIComponent(`⚽ VER EN VIVO:
${matchName}

📺 Mira el partido aquí 👇
`);
const encodedUrl = encodeURIComponent(matchUrl);

const whatsappUrl = `https://api.whatsapp.com/send?text=${encodedText}${encodedUrl}`;
const telegramUrl = `https://t.me/share/url?url=${encodedUrl}&text=${encodedText}`;
---

<div class="share-buttons">
  <p>Compartir este partido:</p>
  <div class="buttons-grid">
    <a href={whatsappUrl} target="_blank" rel="noopener noreferrer" class="btn btn-whatsapp">
      WhatsApp
    </a>
    <a href={telegramUrl} target="_blank" rel="noopener noreferrer" class="btn btn-telegram">
      Telegram
    </a>
  </div>
</div>

<style>
  .share-buttons {
    margin: 1.5rem 0;
    padding: 1rem;
    background: #f9f9f9;
    color: #333;
    border-radius: 4px;
    border: 1px solid #ddd;
  }
  
  .share-buttons p {
    font-weight: bold;
    margin-bottom: 0.5rem;
  }

  .buttons-grid {
    display: flex;
    gap: 10px;
  }

  .btn-whatsapp {
    background-color: #25d366;
  }
  .btn-whatsapp:hover {
    background-color: #128c7e;
  }

  .btn-telegram {
    background-color: #0088cc;
  }
  .btn-telegram:hover {
    background-color: #006699;
  }
</style>
